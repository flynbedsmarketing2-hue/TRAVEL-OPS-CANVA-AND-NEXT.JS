# Public values can be prefixed with NEXT_PUBLIC_ and are safe to expose in the browser.
# Copy this file to .env.local and adjust for your environment.

NEXT_PUBLIC_APP_URL=http://localhost:3000
API_BASE_URL=http://localhost:3000/api
DATABASE_URL="postgresql://travelops:<your-password>@127.0.0.1:5433/travelops?schema=public"

# Run `npm run db:sync-env` if you use the provided Docker Postgres setup to auto-sync credentials from the container.
