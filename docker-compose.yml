services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: travelops
      POSTGRES_PASSWORD: travelops
      POSTGRES_DB: travelops
    ports:
      - "5433:5432"
    volumes:
      - travelops-postgres:/var/lib/postgresql/data

volumes:
  travelops-postgres:
